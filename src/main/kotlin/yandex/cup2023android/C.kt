package yandex.cup2023android

//решено = 0,1,2,0,1,2 //Am, Dm, E, Am, Dm, E

/*
fun main(args: Array<String>) {

    val flow: Flow = flow {
        repeat(3) {
            emit(it)
        }
    }

    runBlocking {
        flow.collect {value -> println(value)}
        flow.collect {value -> println(value)}
    }
}
*/
