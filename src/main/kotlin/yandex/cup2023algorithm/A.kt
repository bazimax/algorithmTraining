package yandex.cup2023algorithm

/**
 * Привет! (:
 * Задачи клёвые, но хотелось бы более понятного описания))
 *
 * Я так и не понял по примерам, как рассчитывается степень красоты (например почему во втором примере 3, а не 2).
 * Непонятно как эти отрезки должны выглядеть, как делиться.
 * Что за "элемент должен входить ровно в один отрезок" - это минимальный элемент, уникальный, последовательность?
 *
 * Хотелось бы в будущем больше пояснений к примерам и более детального описания логики (например, как на leetcode).
 * Тогда можно будет меньше времени тратить на понимание условий и больше на само решение.
 * Спасибо!
 */

fun main(args: Array<String>) {
    smartCatSteven()
}

//не решил - по примерам непонятна логика
fun smartCatSteven() {

}

/*
import java.io.BufferedReader
import java.io.BufferedWriter
import java.io.FileReader
import java.io.FileWriter

const val INPUT = "input.txt"
const val OUTPUT = "output.txt"

fun readFile(pathToInputFile: String): MutableList<String>{

    val file = BufferedReader(FileReader(pathToInputFile))

    val listString = mutableListOf<String>()
    file.lines().forEach { listString.add(it) }

    return listString
}

fun writeFile(string: String, pathToOutputFile: String){

    val output = BufferedWriter(FileWriter(pathToOutputFile))

    output.write(string)
    output.flush()
}*/
